# Fundamental pain points

with treditional js, defining code structure lacks the data flow and type anotation to clarify intent and to make execution deterministic, thus we need to stictly follow and plan out the data flow which results us to make less of such ambiguous build up into large scale applications.

Type script adding a layer for validation for easy bug cach with its debuging engine which makes us flag out the bugs before they happens
