// Industrial level greeter function

function greet(person:string, date:Date){
  console.log(`Hello ${person}, today is ${date.toDateString()}`);
}

// we would try to build basic working units for the chaitanya

greet("Madhav Rimal", new Date)
