Making more features as zero as possible to make sure we don't complicate the dataset and feature study,

Assumption : Some feature might be dependent on other features linear combination and does't adds meaning to have a weight for that feature, 

Taking only required feature for required features to train the final model

With added minimization constraints we introduce a new theta constrains for the original problem for w to be selected with,