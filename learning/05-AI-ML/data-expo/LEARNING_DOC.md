# Ploting with matplot

Overview Structure of making plot
load data -> Render the graphics with its commands for interactivity

## Simple steps to make a plot
1. Load the data into Pandas data frame for easy grabing and analysis
2. initiating plots essentials  : fig, ax = plt.subplots()  <-- figure and ax elements for figure customization
3. Making sure whih data for x and y axis this could lead to weird behaviour thus be sure with loading graph

