LLM Context with the web development
Node ecosystem is the essential backbone about the web development being native to this domain it gives the all given minute detailed control about the application.
Thus the complication grows with the given context of application.

# Environment
Node talks with file system and network like normal interpreter to server the servers.

# Global Object
server essential stuff stored into the global object into the server thing in node replacing window thing from web browser

# Execution Flow [ single threaded ]
Single threaded from core, but reading file, network calls and DB query is passed into the OS to handle and just takes responses from all of these.

Blocking system
	due to single threaded way if one user blocks with some sort of computational thing then it would stop server for all other users.
	package-lock makes the snapshot and package.json makes the requriement.txt for the project
	
# Network
Node request handle unit working
Request breakdown
  + url/path : which endpoint to hit
  + method : Which method is required
  + headers : Information about the request required
  
  user connects
  	raw network socket [ continuous data pipeline ] 
  	
express way
making simple interface for using node js raw power to handle the server requirements and making taks deligation into simplification.

