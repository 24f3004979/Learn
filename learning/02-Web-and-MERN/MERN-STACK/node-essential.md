# Async Execution Flow
making async code execution ~ Executing other operations while waiting for one
sync : Execution into order | Async making other operations work while one process  completes itsel

JS Core : Its syncronous programming language but it can go async for its certain features like callback and making the web page living by geting note of event loops and user interaction call track.

# Callback 
Making callback, order a samosa, you wont be needing to stay to get it once its ready you would be called, thus the way with callback, we pass a function into a function when things are ready the next function is called with the requried information.
