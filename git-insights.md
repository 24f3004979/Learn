# GIT Working

+ Making simple workflow about the github
+ Different Binary tree for storage options , like storing 10 at root, instead of storing 20 in next storing 10 for refrence of 20 which could be computed.
+ Dynamic prgramming, threaded programming, these are legendary topics i have not touched the tip with dsa yet have to explore these domain for the given topics
+ Understand the overview of git working with the documentation explaining the design of the github.

Ceizer cypher algorithm for making given shasum for the given information :)

+ Legendary Programming book refrence by prakash sir


Github working mechanism
a version of code + one comma -> it would only store the change not the whole code but it can retrive the information with building it with given history about the change

Python Code with protected variables
    _name -> only accessed within the class and its subclass : Protected variable
    __name -> only in defining class scope ~ Made for the extreamly consise about the scope
